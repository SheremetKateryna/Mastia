mixin service
	section.service
		.layout_row
			.layout_col-3
				each item in serviceItems
					.service_item
						i.fab.fa-slack-hash
							h5.service_title= item.title
							.service_text= item.text
			.layout_col-3
				each item in serviceItems
					.service_item
						i.fab.fa-slack-hash
							h5.service_title= item.title
							.service_text= item.text
			.layout_col-3
				each item in serviceItems
					.service_item
						i.fab.fa-slack-hash
							h5.service_title= item.title
							.service_text= item.text