    $(document).ready(function(){
      $('.header-slider').slick({
        arrows: true,
        // dots: true,
        // nextArrow: '._nextArrow',
        // prevArrow: '._prevArrow',
      });
    });